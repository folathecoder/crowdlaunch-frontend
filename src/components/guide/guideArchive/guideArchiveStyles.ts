import styled from 'styled-components';

export const ArchiveContainer = styled.main`
  max-width: 100%;

  /* background: radial-gradient(
        circle at 40% 60%,
        var(--color-accent-100) -70%,
        var(--color-bg-100) 25%
      )
      top/100% 50%,
    radial-gradient(
        circle at 43% 30%,
        var(--color-accent-100) -20%,
        var(--color-bg-100) 20%
      )
      bottom/100% 50%; */
`;
