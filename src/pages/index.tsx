import type { NextPage } from 'next';
import { HomePageTemplate } from '@/components/home';

const Home: NextPage = () => {
  return (
    <main>
      <HomePageTemplate />
    </main>
  );
};

export default Home;
